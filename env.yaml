name: warp-fwuav
channels:
  - conda-forge
dependencies:
  - python=3.10
  - pip
  - numpy
  - scipy
  - pyyaml
  - matplotlib
  - pytest
  - pyvista
  - pip:
      # CUDA-enabled JAX (matches your working env example).
      - "jax[cuda12]"
      - warp-lang
      - xlb
      - -e .
